# Verify: obserwowalność produkcyjna · spec 0021 · updated 2026-09-02

_Steps derived from spec 0021 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

Prerequisite for every dashboard-facing step below: a real Sentry project (EU/Frankfurt region) and a real PostHog project (EU Cloud) must exist, with `SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_URL`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` set on Vercel's staging and production environments only (build plan step 1). **Done 2026-09-02**: all 8 variables added by the engineer to Vercel's Production and Preview environments (Preview stands in for Staging — real Custom Environments needs Vercel Pro, not yet purchased, spec 0019 Follow-up; `resolveEnvironment()` already treats `VERCEL_ENV=preview` as `staging`). Development and CI remain unconfigured by design.

**Update 2026-09-02**: both tools are now provisioned. Sentry: org `modularhub-europe`, dedicated project `modularhub-europe` (DSN host `ingest.de.sentry.io` confirming EU/Frankfurt region; the initial shared `javascript-nextjs` project, which had a pre-existing unrelated issue from another app, was retired). PostHog: EU Cloud project (`eu.posthog.com`, project ID `263384`). `.env.local` has every value except nothing outstanding. Sentry auth token added by the engineer directly.

**Finding (resolved)**: the first Sentry project tried (`javascript-nextjs`) turned out to be shared with another app (a pre-existing "Hydration Error" issue from `menu-gn.vercel.app`). Created a dedicated `modularhub-europe` project instead and moved the DSN over; the old project was deleted by the engineer 2026-09-02 (no delete-project tool in the Sentry MCP server, so this step was manual).

**Finding**: Sentry attaches `user.geo` (city-level, IP-derived) to captured events even though `scrubSentryEvent()` reduces the `user` object to `{ id }` before sending — this is Sentry's own server-side IP geolocation on ingestion, not something the SDK payload controls. If this needs to be off too, it's a per-project Sentry setting (Settings → Security & Privacy → "Store IP Address"), not a code change.

## UI / manual

- [x] Throw an error from a Server Action (Route Handler) → appears in the Sentry EU dashboard with `path`, `release` (matched the current commit SHA `8b87410c…`), and `role`/`user.id` tags; no email, name, phone, address, payment, or document field present → AC-1 — verified 2026-09-01 (then re-verified 2026-09-02 against the dedicated project as `MODULARHUB-EUROPE-1`) via a temporary `/api/observability-test` route, removed after each use
- [x] Throw an error from a Client Component (browser) → appears in Sentry with browser/device info → AC-1 — verified 2026-09-02 (`/check verify`): a temporary page (`app/[locale]/observability-test-client/page.tsx`, removed after use) threw post-hydration in a `useEffect`-driven re-render, purely client side (no HTTP 500, no server round trip). Landed as `MODULARHUB-EUROPE-4`, `platform: javascript`, `mechanism: auto.browser.global_handlers.onerror`, tags `browser: Chrome 152.0.0`, `os: Windows >=10`; no email/name/phone/address/payment/document field present (only the already-known `user.geo` IP-geo finding above)
- [x] Trigger a proxy/route error → appears in Sentry → AC-1 — exercised 2026-09-02 (`/check verify`): temporarily threw inside `proxy.ts` behind a one-off path check, hit it, reverted immediately. **Finding**: the error DID reach Sentry (`MODULARHUB-EUROPE-5`, real Node stack trace ending at `proxy.ts:7:11`) but NOT via the expected `onRequestError` server-side path — its tags are `platform: javascript`, `mechanism: auto.browser.browserapierrors.setTimeout`, i.e. the same client-side auto-capture as an ordinary browser error, not a `route_type`/`router_kind`-tagged server capture like the SSR-render error above got. No `routeType: proxy` tag exists on any event. This looks like Next.js's dev-mode error overlay re-throwing the server error's message/stack client-side for display, which the browser SDK then auto-captured — a coincidental dev-only path, not proof `onRequestError`/`Sentry.captureRequestError` actually fires for proxy/middleware errors. **Unresolved** → route to `/debug`: confirm whether `captureRequestError` covers proxy errors at all in Next.js 16 (dev and build), and whether this holds in a production build, not just `next dev` with Turbopack
- [ ] With `SENTRY_DSN` unset (the default local/CI state) → thrown errors do not appear in any Sentry project → AC-2 — not live re-verified 2026-09-02 (would need a second dev server process, blocked by Next's single-dev-server-per-project lock in this session); code path is a direct `dsn: process.env.SENTRY_DSN` passthrough to `Sentry.init()`, `dsn: undefined` is documented upstream SDK behavior to disable sending — low priority given the structural guarantee, same as previously noted
- [x] Sentry's default "Send a notification for high priority issues" rule (email → active members, on new or escalating high-priority issues) is enabled on the project → AC-3 — confirmed enabled via Sentry MCP 2026-09-01; re-confirmed 2026-09-02 (`/check verify`), rule id `798955`, `lastTriggered: 2026-09-02T10:08:19Z`, i.e. it has already fired for real once; actual email delivery still not independently confirmed (no inbox access from this session)
- [x] Call `trackEvent(...)` once for each of the seven event names, then `identify(userId, "klient", distinctId)` with the same ID → all seven events and the identity link appear under one person in the PostHog EU project → AC-4 — verified 2026-09-02 via a temporary `/api/observability-test` route (removed after use); PostHog Activity showed all 8 entries (7 named events + `Identify`) under person `verify-test-distinct-id`, `library: posthog-node`
- [ ] With `NEXT_PUBLIC_POSTHOG_KEY` unset → `trackEvent()` calls produce no events in any PostHog project → AC-2 — not live re-verified 2026-09-02, same reasoning and same priority as the unset-`SENTRY_DSN` line above (`getClient()` returns `null` when the key is falsy, a direct guard, not independently re-exercised live)
- [x] Temporarily point `NEXT_PUBLIC_POSTHOG_HOST` at an unreachable host and call `trackEvent()` during a real action → the action still completes normally; only the console shows `[observability] PostHog flush failed` → AC-6 — verified 2026-09-02 (`/check verify`) via a temporary vitest spec (`lib/observability/_verify-unreachable-host.test.ts`, `@vitest-environment node`, `server-only` mocked, removed after use) that imported the real `trackEvent()` with `NEXT_PUBLIC_POSTHOG_HOST=http://127.0.0.1:9`: the call returned in 1ms (never blocks), and `console.error("[observability] PostHog flush failed", …)` fired ~9s later once posthog-node's own retry/backoff gave up with "Network error while fetching PostHog" — nothing thrown back to the caller
- [x] Search `lib/`, `app/`, `components/` for `@sentry/nextjs`, `posthog-js`, or `posthog-node` imports outside `lib/observability/`, `instrumentation*.ts`, `sentry.*.config.ts`, `next.config.ts` → none found; confirm `npm run lint` also fails when one is added deliberately (already verified once during this build) → AC-5 — re-verified 2026-09-02 (`/check verify`): grep found exactly the 6 exempt files (`lib/observability/events.ts`, `errors.ts`, `instrumentation.ts`, `instrumentation-client.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`) and nothing else; a deliberately added `import * as Sentry from "@sentry/nextjs"` in a throwaway file failed lint with the exact `no-restricted-imports` message, file removed after
- [ ] Once real projects exist: delete or anonymize a test pseudonymous ID's history via Sentry's search-and-delete-by-tag and PostHog's person deletion API, once each → AC-7 — **checked 2026-09-02: still missing, and no longer just blocked.** Real Sentry and PostHog projects have existed since 2026-09-02 (see the "Update 2026-09-02" note above), so the earlier "blocked, no real projects yet" reason no longer applies. `docs/` has no runbook document (`grep -rli "anonymiz|erasure|runbook|deletion" docs/` finds nothing but this spec's own files), and no deletion/anonymization has been exercised. This is a scope miss, route to `/develop`
- [~] In the Sentry and PostHog dashboards, confirm the trace sample rate is capped (20% in production) and Sentry's spike protection is on (default, not disabled) → AC-8 — partially checked 2026-09-02: `tracesSampleRate: environment === "production" ? 0.2 : 1.0` confirmed identically in all three Sentry entry points (`sentry.server.config.ts`, `sentry.edge.config.ts`, `instrumentation-client.ts`) by reading the code, so the 20% production cap is real; spike protection has no exposed API/MCP tool in this session to independently confirm the dashboard toggle itself (it is Sentry's own default, per the prior session's finding above, not re-confirmed live this time)

## Commands

- [x] `npm run build` → succeeds, `withSentryConfig` reports a release computed from the commit SHA in the build output → AC-1 (release tagging) — verified 2026-09-02: build succeeds (`next.config.ts` sets `silent: !process.env.CI`, so no build-log line is expected locally); confirmed the release exists server-side instead, via Sentry MCP `find_releases`: version `8b87410c9ee8b5f22336e907720aa2a8abe61b73` (current `HEAD`) present in the `modularhub-europe` project with a real `lastCommit` and `dateReleased`
- [x] `npm run lint` → passes; adding a direct `import * as Sentry from "@sentry/nextjs"` to a file outside the exempt list fails lint with the `no-restricted-imports` message → AC-5 — verified 2026-09-02, see the grep/lint line above
- [x] `npm run test` → `lib/observability/scrub.test.ts` and `lib/observability/environment.test.ts` pass → AC-1, AC-2 (unit-level) — verified 2026-09-02: full suite green, 61 files / 415 tests passed, including both files

## Acceptance-criteria coverage

- AC-1 (error capture + context, PII excluded) · covered by the throw-and-inspect steps and `scrub.test.ts` · server-side, client-side, and build/release paths all confirmed live against the real Sentry EU project as of 2026-09-02; the proxy path reaches Sentry but not demonstrably via `onRequestError`/`captureRequestError` (see finding above) — treat AC-1 as met for server and client code, open for the proxy surface specifically
- AC-2 (environment tagging, dev/CI silent) · covered by the unset-DSN/key steps and `environment.test.ts` · code unit-verified; live event confirmed tagged `environment: development`; Vercel now scoped to Production + Preview only, dev/CI have no keys by construction (the unset-DSN/key manual re-check is still open, low priority given the structural guarantee)
- AC-3 (email alert) · covered by the alert-rule step · rule confirmed enabled and confirmed to have fired at least once live (`lastTriggered` 2026-09-02); email delivery itself still unverified (no inbox access)
- AC-4 (trackEvent + identify, explicit distinctId) · covered by the seven-event step · fully verified live against the real PostHog EU project
- AC-5 (single wrapper, lint enforced) · covered by the import-search and lint steps · fully verified, re-confirmed live 2026-09-02
- AC-6 (never blocks, flushed via `waitUntil()`, failure logged not thrown) · covered by the unreachable-host step · fully verified live 2026-09-02: non-blocking call confirmed (1ms), failure-logged-not-thrown confirmed (`console.error` fired ~9s later, nothing thrown)
- AC-7 (deletion/anonymization runbook, exercised once) · covered by the deletion step · **not met**: real projects now exist so the earlier "blocked" reason is gone; no runbook document exists and no deletion/anonymization has been exercised — a real scope miss, route to `/develop`
- AC-8 (sampling + spike protection, quota safe) · covered by the dashboard config check · code confirms a 20% production trace cap identically across all three Sentry entry points; spike protection is Sentry's own default per the prior session's finding, not independently re-confirmed live this session (no dashboard-settings tool available)
